# Next Mission

## 重要说明
- 本文件用于描述本次迭代的需求与约束，所有开发工作应以本文件为目标。
- 在开始编码前，请先阅读 `dev-guidelines/rules/` 目录下的规则文件。
- 当前分支：**framework**（用于新项目框架演示，内容简化）。

## 版本与迭代
- **当前版本**：v0.4.0
- **当前迭代**：Iteration 6（按键中断与非阻塞消抖硬件验收）

## 参考文档
- 规则文件（核心）：`dev-guidelines/rules/`
- 项目结构说明：`docs/README.md`
- 开发流程说明：`docs/development.md`

---

## 本次目标

### 任务 1：按键软件基线
- KEY1～KEY4 固定映射为 PA8、PB15、PB14、PB13，均使用内部上拉和下降沿 EXTI，按下为低电平。
- EXTI ISR 只清除硬件标志并设置软件待处理位，禁止在中断中消抖、阻塞或调用业务回调。
- 主循环持续调用 `KEY_Task()`，通过 SysTick 时间差完成 20 ms 非阻塞按下/松开消抖。
- 稳定按下后只执行一次已注册回调；按住不重复触发，稳定松开后允许下一次按下。

### 任务 2：硬件专项验收
- 分别按下 KEY1～KEY4，确认只触发对应预留回调，其他按键不误触发。
- 快速点按、抖动按压和长按测试中，确认每次有效按下只产生一次回调。
- 松开稳定后再次按下，确认能够再次触发；同时或交错操作多个按键，确认共享 EXTI 中断不丢失事件。
- 按键任务和回调必须保持非阻塞，联合运行时确认三相 PWM 相位、ADC 采样和主循环处理不受影响。

---

## 备注（可选）
- 只依赖 Keil + EIDE 编译工具链，不引入其他构建系统。
- 目标平台：GD32F303CC
- 任何修改都应确保在硬件上可测试。

