---
description: GD32F303 项目特定规则
globs: **/*.[ch]
alwaysApply: false
---

# 项目规则

## 代码风格与命名
- 函数：`Module_FunctionName`（例如：`Timer_Init`、`PWM_SetDutyCycle`）
- 宏：`UPPERCASE_WITH_UNDERSCORES`
- 类型：`xxx_t` 后缀
- 遵循现有模式，除非重构

## 模块边界
- 每个 `Utilities/*` 模块都是自包含的
- 通过头文件提供干净的 API
- 避免跨模块全局变量
- 每个模块应有明确的职责划分，命名应语义化而非参数化

## 优先级与文档来源
- **关键依据**：代码本身（`.c/.h`）应始终是最终事实
- 次级参考：`docs/` 目录下的说明文档
- 规则文件（`dev-guidelines/rules/*`）用于指导 AI 和协作开发

## 可编辑范围
- ✅ `examples/*/software/Application/`
- ✅ `examples/*/software/Utilities/**`
- ✅ 文档：`docs/`、`NEXT_MISSION.md`、`CHANGELOG.md`
- ❌ 推荐不修改：`examples/*/software/Firmware/**`、`.eide/**`（仅在必要时修复构建/烧录问题）

## 中断处理
- ISR 应保持精简，只做必要工作
- 在 ISR 中尽量避免阻塞操作；若必须阻塞，必须明确保证不会影响系统实时性
- ISR 应尽量转发到模块处理函数，避免直接包含复杂逻辑

## 时序与调度
- 多速率任务调度由 L1 编排层统一管理
- 任务命名应使用功能语义（如 `FastControlLoop`、`StateMonitor`），而非频率参数
- 使用 DWT 等硬件计时工具监控关键路径执行时间
- 定时器初始化应参数化

## 执行时间预算
- 任何高频率任务都应确保最坏情况执行时间远低于调度周期
- 使用 DWT 周期计数器进行检查
- 尽量避免在初始化/校准路径中使用阻塞延时
- 如果必须使用阻塞延时，应明确说明执行时间影响，并确保不会影响关键实时任务

## 安全性
- 访问指针前校验，不使用未初始化变量
- 需要 `NULL` 时必须包含 `<stddef.h>`
- 未经任务批准，避免修改时钟、内存映射和底层硬件配置

## 构建与烧录
- 主工具链：Keil μVision 5 + ARM Compiler 5 (AC5)
- 开发环境：VS Code + EIDE 扩展用于编译
- 烧录/调试：ST-LINK（用户手动操作，AI 不进行烧录或调试）
- 发现编译警告即应处理，多数警告反映潜在问题

## 迭代与文档契约
- 只有在用户明确要求"进行下一次迭代"时，才认为是新迭代
- 每次迭代完成后需更新：
  - `CHANGELOG.md`（版本历史）
  - `NEXT_MISSION.md`（下一次目标）
  - `docs/` 中的相关说明
  - 规则文件（若流程/架构变化）