---
description: ARM Cortex-M项目的通用嵌入式开发规则
globs: **/*.[ch]
alwaysApply: false
---

# 通用嵌入式开发规则

## 资源优化
- 最小化RAM/ROM使用
- 优先静态分配而非动态
- 尽可能使用定点数学，但允许在性能敏感任务（ISR、ADC 处理等）中使用浮点运算（如果存在FPU）
- 根据调试反馈和目标是否存在FPU单元，调整浮点运算使用频率
- 分析代码大小和执行时间

## 中断安全性
- 保持ISR简短且确定性
- 对共享变量使用volatile
- ISR中避免浮点运算
- 优先使用事件标志而非轮询

## 外设处理
- 按正确顺序初始化外设
- 对批量数据传输使用DMA
- 优雅处理错误条件
- 记录时序要求

## 代码质量
- 使用有意义的变量名
- 为API添加Doxygen注释
- 避免魔数
- 为调试使用断言
- 对于ISR/DMA交互的数据，使用 `volatile` 并命名为 `x_ready` / `x_flag`
- 注意 `volatile` 不等于原子访问，需要适当屏蔽中断或使用原子操作

## 工具链考虑
- 注意编译器优化
- 使用适当的数据类型
- 正确处理字节序
- 需要时验证生成的汇编

## 测试策略
- 尽可能对工具进行单元测试
- 硬件验证是强制性的
- 对时序使用示波器/逻辑分析仪
- 记录测试程序